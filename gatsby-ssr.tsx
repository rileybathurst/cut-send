// ? why am I running this?
// I can do the html attribute through the seo file I believe?
// I kinda bet this is killing my forms

/* exports.onRenderBody = ({ setHtmlAttributes }) => {
  setHtmlAttributes({
    lang: "en"
  })
} */